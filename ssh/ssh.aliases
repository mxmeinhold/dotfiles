function agentize () {
    INFO="\e[1m\e[93m"
    RESET="\e[0m"
    echo -e "${INFO}--->  Entering ssh-agent${RESET}"
    ssh-agent bash 
}

if [ "$(ssh-add -l 2> /dev/null)" == "The agent has no identities." ] ; then
    ssh-add ~/.ssh/id_rsa
fi
