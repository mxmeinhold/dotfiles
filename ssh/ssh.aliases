function agentize () {
    INFO="\e[1m\e[93m"
    RESET="\e[0m"
    echo -e "${INFO}--->  Entering ssh-agent${RESET}"
    IS_AGENT=1 ssh-agent bash 
}

if [ $IS_AGENT ]; then
    ssh-add ~/.ssh/id_rsa
fi
